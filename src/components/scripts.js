// Define the animation function
function runAnimations() {
    console.log("Running animations...");
  }
  
  // Export `runAnimations` as the default export
  export default runAnimations;
  
  // Define and export the links object
  export const allLinks = {
    // Add any link mappings here if needed
  };
  
  // Define and export the functions object
  export const allFunctions = {
    // Add any additional functions here if needed
  };
  